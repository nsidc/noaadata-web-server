version: '3.4'

services:

  data-server:
    image: "nsidc/noaa-web-server:${SERVER_VERSION:-latest}"
    volumes:
      - 
    ports:
      - "80:80"
      - "443:443"
    logging:
      options:
        max-size: "10m"
        max-file: "10"
